#include <iostream>
#include <cstdlib>
using namespace std;
	
int main() {
	//Escreva um programa que leia o lado de um quadrado e calcule a area A= ladoxlado
	float L, Area;
	cout<<("Digite o valor do Lado do quadrado em: ");
	cin>>(L);
	Area = L * L;
	cout<<(" A area do quadrado e: ");
	cout<<(Area);
}